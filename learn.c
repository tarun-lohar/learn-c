#include <stdio.h>

float sum(float a, float b) { return a * b; }

int main()
{
    int i = 0;
    while ("kfyjg")
    {

        if (i > 10)
        {
            break;
        }

        printf("%d\n", i);
        i = i + 2;
    }
}
